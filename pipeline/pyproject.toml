[project]
name = "fly-game-pipeline"
version = "0.1.0"
description = "Подготовка коннектома FlyWire для браузерной игры"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0",
    "pandas>=2.2",
    "pyarrow>=16",
]

[tool.uv]
package = false
